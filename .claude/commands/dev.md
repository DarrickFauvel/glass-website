Start a local dev server for the site.

1. Run `pnpm dev` in the project root (foreground/background as appropriate). Ensure pnpm is on PATH (`export PATH="$HOME/.local/share/pnpm/bin:$PATH"` if needed).
2. Tell me the URL: http://localhost:8080
